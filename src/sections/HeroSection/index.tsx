import { HeroCarousel } from "@/sections/HeroSection/components/HeroCarousel";

export const HeroSection = () => {
  return (
    <section className="relative isolate z-0 w-full h-screen overflow-hidden">
      <HeroCarousel />
    </section>
  );
};
